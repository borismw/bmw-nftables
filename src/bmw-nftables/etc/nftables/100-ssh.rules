#! /usr/bin/nft -f
# vim:set ts=2 sw=2 et:

table inet firewall {
  chain input_hook {
    tcp dport 22 counter jump ssh_filter
  }

  chain ssh_filter {
    ip saddr @ADDR_TRUSTED counter accept
  }
}
