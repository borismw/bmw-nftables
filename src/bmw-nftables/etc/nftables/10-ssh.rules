#! /usr/bin/nft -f
# vim:set ts=2 sw=2 et:

table inet firewall {
  chain ssh_filter {
    ip saddr @trusted_wifi counter accept
  }
  
  chain base_filter {
    tcp dport 22 counter jump ssh_filter
  }
}

